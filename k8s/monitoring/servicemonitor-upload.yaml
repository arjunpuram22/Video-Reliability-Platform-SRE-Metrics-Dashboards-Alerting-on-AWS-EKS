apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: upload-service
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - app
  selector:
    matchLabels:
      app: upload-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
